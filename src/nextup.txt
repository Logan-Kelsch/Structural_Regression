make a function that will take in the scored indices s_idx and it will find all family indices, it will then determine which if s_idx have other s_idx within their structure, they are automatically no longer considered. this will leave us with a smaller set of s_idx, with this set of s_idx we will get all family indices (now smaller set) and then for each index (node) we will determine if there are multiple remaining s_idx that used that node, we will assign the best score to that node, then for all remaining s_idx nodes they of course get their own score that comes in in the original array sidx_scores that is parallel with original s_idx. this allows the entire reduced s_idx and family array/list of indices to have a parallel array of scores. we get family index from the function _I.family_tree_indices(X._instructions, single_index_or_list_of_indices_object_here). this function then will return the two parallel lists of scores and parallel list of entire family indices.

we will expect to have iterative tq1d grammar that explodes in a 
negative manner possibly within one single iteration. we will grab 
some visuals of this, then we will move onto a mcts approach to dodge 
greedy usage of variables.
